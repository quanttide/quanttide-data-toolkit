[package]
name = "quanttide-data"
version = "0.1.0"
edition = "2021"
authors = ["QuantTide Inc. <opensource@quanttide.com>"]
description = "量潮数据工程核心库 — Blueprint 数据模型与 CUE 解析"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"

[dev-dependencies]
serde_test = "1"
